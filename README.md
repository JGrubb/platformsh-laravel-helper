# platformsh-laravel-helper

A simple package to bootstrap all of the stuff that would normally live in the `.env` file.  Only Does Stuff when you deploy your Laravel app to [Platform.sh](https://platform.sh/)